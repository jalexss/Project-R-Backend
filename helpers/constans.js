const ratingValues = [0, 1, 2, 3, 4, 5];

const allRoles = ["admin", "client", "super-admin", "moderator"];
const highRoles = ["admin", "moderator", "super-admin"];

const statusUser = ["active", "pending", "banned"];

module.exports = {
  allRoles,
  highRoles,
  statusUser,
  ratingValues,
};
